- defaults:
    link:
      create: true
      relink: true

- clean: ['~']

- link:
    ~/.bashrc: bashrc
    ~/.config: config

  # - create:
  #     - ~/downloads
  #     - ~/.vim/undo-history

- shell:
  - [git submodule update --init --recursive, Installing submodules]

